# Deprecated: replaced by e2e.yml
# Manual dispatch only — points operators to the new E2E workflow.

name: "Full System Docker E2E (legacy alias)"

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Use workflow E2E instead"
        required: false
        default: "Prefer .github/workflows/e2e.yml"

jobs:
  redirect:
    name: Use E2E workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::docker-full-system.yml is deprecated. Use E2E (.github/workflows/e2e.yml)."
