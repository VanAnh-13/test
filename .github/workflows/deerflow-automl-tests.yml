# Deprecated: replaced by ci.yml
# Kept as a thin alias so old branch protections / badges can redirect.

name: DeerFlow-AutoML Tests (legacy alias)

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Use workflow CI instead — this only re-dispatches"
        required: false
        default: "Prefer .github/workflows/ci.yml"

jobs:
  redirect:
    name: Use CI workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::deerflow-automl-tests.yml is deprecated. Use CI (.github/workflows/ci.yml)."
          echo "Open Actions → CI for unit/phase tests and offline eval harness."
