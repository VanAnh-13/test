# Deprecated: docs deploy lives in cd.yml

name: Deploy Documentation (legacy alias)

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Use CD workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::deploy-docs.yml is deprecated. Use CD (.github/workflows/cd.yml)."
